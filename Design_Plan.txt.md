- “分门别类”，我们需要将文件系统的**目录结构**映射为网站的**分类层级**。这样您只需在 `content/` 下创建文件夹，网站就能自动识别并生成分类导航，无需额外配置。

  以下是升级后的完整方案，支持 **多级分类**（如“经部/易类”、“子部/道家”），且依然保持纯静态、自动构建的特性。

  ------

  ## 📁 升级版文件架构（文件树）

  text

  ```
  lou-says/
  ├── content/                              # 🗂️ 根内容目录（您只需维护此目录）
  │   ├── 经部/                             # 一级分类
  │   │   ├── 易类/                         # 二级分类（可选）
  │   │   │   ├── 2026-01-01-乾卦解读.md
  │   │   │   └── 2026-01-05-坤卦浅析.docx
  │   │   └── 书类/
  │   │       └── 2026-01-10-尚书选读.md
  │   ├── 子部/
  │   │   ├── 儒家/
  │   │   │   └── 2026-02-01-孟子心性论.docx
  │   │   └── 道家/
  │   │       └── 2026-02-05-道德经注译.md
  │   └── 集部/
  │       └── 楚辞/
  │           └── 2026-03-01-离骚赏析.md
  │
  ├── public/                               # 静态资源
  │   └── data.json                         # (构建时自动生成)
  │
  ├── src/
  │   ├── components/
  │   │   ├── Layout.jsx                    # 整体布局（含侧边栏）
  │   │   ├── Sidebar.jsx                   # 分类树导航
  │   │   ├── ArticleList.jsx               # 文章列表（支持按分类过滤）
  │   │   └── ArticleDetail.jsx             # 文章详情页
  │   ├── utils/
  │   │   └── helpers.js                    # 辅助函数
  │   ├── App.jsx
  │   ├── main.jsx
  │   └── index.css
  │
  ├── scripts/
  │   └── build-data.js                     # 构建脚本（递归扫描+分类提取）
  │
  ├── package.json
  ├── vite.config.js
  └── .gitignore
  ```

  

  ------

  ## 🧠 分类逻辑设计

  - **分类来源**：文件相对于 `content/` 的**相对路径**。
    - 例如：`content/经部/易类/乾卦解读.md` → `category: "经部/易类"`
    - 例如：`content/子部/道家/道德经注译.md` → `category: "子部/道家"`
  - **前端展示**：侧边栏自动生成可折叠的树形分类目录。
  - **URL路由**：支持按分类过滤（例如 `/?category=经部/易类`），同时保留独立详情页 `/article/:slug`









